# 韓国語学習アプリ 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** iPhoneのSafariで使える自分用韓国語学習シングルページアプリを作る

**Architecture:** HTML/CSS/JSのシングルページアプリ。タブ切り替えで単語・フレーズ・テスト・履歴を表示。学習進捗はlocalStorageに保存。データはwords.jsとphrases.jsに分離。

**Tech Stack:** HTML5, CSS3, Vanilla JavaScript, Canvas API, localStorage

---

## ファイル構成

```
03_PRODUCTS/01_韓国語学習/
├── index.html          # アプリ本体（全画面をJSで切り替え）
├── style.css           # スタイル（iPhone Safari対応）
├── app.js              # アプリロジック（タブ制御・テスト・履歴）
└── data/
    ├── words.js        # 単語データ50語
    └── phrases.js      # フレーズデータ（K CHOICE! 30件＋基本20件）
```

---

### Task 1: フォルダ構成とHTMLスケルトン

**Files:**
- Create: `03_PRODUCTS/01_韓国語学習/index.html`
- Create: `03_PRODUCTS/01_韓国語学習/data/` (フォルダ)

- [ ] **Step 1: フォルダを作成する**

```bash
mkdir -p /Users/cratermacbookpro/Projects/claude/03_PRODUCTS/01_韓国語学習/data
```

- [ ] **Step 2: index.htmlを作成する**

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>韓国語学習</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- メインコンテンツ -->
  <main id="app">

    <!-- 単語タブ -->
    <section id="tab-words" class="tab-content active">
      <h2 class="page-title">単語</h2>
      <div id="words-list"></div>
    </section>

    <!-- フレーズタブ -->
    <section id="tab-phrases" class="tab-content">
      <h2 class="page-title">フレーズ</h2>
      <div id="phrases-list"></div>
    </section>

    <!-- テストタブ -->
    <section id="tab-test" class="tab-content">
      <h2 class="page-title">テスト</h2>
      <div id="test-mode-select">
        <button class="mode-btn active" data-mode="ja-to-ko">日→韓</button>
        <button class="mode-btn" data-mode="ko-to-ja">韓→日</button>
        <button class="mode-btn" data-mode="handwrite">手書き</button>
      </div>
      <div id="test-area"></div>
    </section>

    <!-- 履歴タブ -->
    <section id="tab-history" class="tab-content">
      <h2 class="page-title">履歴</h2>
      <div id="history-area"></div>
    </section>

  </main>

  <!-- ボトムナビ -->
  <nav id="bottom-nav">
    <button class="nav-btn active" data-tab="words">
      <span class="nav-icon">📖</span>
      <span class="nav-label">単語</span>
    </button>
    <button class="nav-btn" data-tab="phrases">
      <span class="nav-icon">💬</span>
      <span class="nav-label">フレーズ</span>
    </button>
    <button class="nav-btn" data-tab="test">
      <span class="nav-icon">✏️</span>
      <span class="nav-label">テスト</span>
    </button>
    <button class="nav-btn" data-tab="history">
      <span class="nav-icon">📊</span>
      <span class="nav-label">履歴</span>
    </button>
  </nav>

  <script src="data/words.js"></script>
  <script src="data/phrases.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 3: 動作確認**

ブラウザで `index.html` を開き、白いページが表示されることを確認する（エラーなし）

- [ ] **Step 4: コミット**

```bash
cd /Users/cratermacbookpro/Projects/claude
git add 03_PRODUCTS/01_韓国語学習/index.html
git commit -m "韓国語学習：HTMLスケルトン作成"
```

---

### Task 2: CSSスタイル・タブナビゲーション

**Files:**
- Create: `03_PRODUCTS/01_韓国語学習/style.css`

- [ ] **Step 1: style.cssを作成する**

```css
/* ベースリセット */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
  background: #f9f9f9;
  color: #222;
  overflow-x: clip;
  padding-bottom: 80px;
}

/* ページタイトル */
.page-title {
  font-size: 20px;
  font-weight: 700;
  padding: 20px 16px 12px;
  color: #111;
}

/* タブコンテンツ */
.tab-content {
  display: none;
  min-height: calc(100vh - 80px);
}
.tab-content.active {
  display: block;
}

/* ボトムナビ */
#bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 72px;
  background: #fff;
  border-top: 1px solid #e5e5e5;
  display: flex;
  z-index: 100;
}

.nav-btn {
  flex: 1;
  border: none;
  background: transparent;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  cursor: pointer;
  color: #aaa;
  font-size: 10px;
  padding-bottom: env(safe-area-inset-bottom);
}

.nav-btn.active {
  color: #1a1a1a;
}

.nav-icon {
  font-size: 22px;
}

.nav-label {
  font-size: 10px;
}

/* K CHOICE! バッジ */
.badge-kchoice {
  display: inline-block;
  background: #e53e3e;
  color: #fff;
  font-size: 9px;
  font-weight: 700;
  padding: 2px 6px;
  border-radius: 10px;
  margin-left: 6px;
  vertical-align: middle;
  letter-spacing: 0.3px;
}

/* カード共通 */
.card {
  background: #fff;
  border-radius: 12px;
  padding: 16px;
  margin: 8px 16px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.08);
  cursor: pointer;
}

.card-hangul {
  font-size: 24px;
  font-weight: 700;
  color: #111;
  margin-bottom: 4px;
}

.card-reading {
  font-size: 13px;
  color: #888;
  margin-bottom: 4px;
}

.card-japanese {
  font-size: 15px;
  color: #444;
}

/* 覚えたボタン */
.btn-learned {
  display: inline-block;
  margin-top: 10px;
  padding: 6px 14px;
  border-radius: 20px;
  border: 1.5px solid #ccc;
  background: transparent;
  font-size: 13px;
  color: #666;
  cursor: pointer;
}

.btn-learned.learned {
  background: #e6f4ea;
  border-color: #4caf50;
  color: #2e7d32;
}

/* フィルターバー */
.filter-bar {
  display: flex;
  gap: 8px;
  padding: 8px 16px;
  overflow-x: auto;
  scrollbar-width: none;
}
.filter-bar::-webkit-scrollbar { display: none; }

.filter-btn {
  white-space: nowrap;
  padding: 6px 14px;
  border-radius: 20px;
  border: 1.5px solid #ddd;
  background: #fff;
  font-size: 13px;
  color: #555;
  cursor: pointer;
}

.filter-btn.active {
  background: #111;
  border-color: #111;
  color: #fff;
}

/* テストモード選択 */
#test-mode-select {
  display: flex;
  gap: 8px;
  padding: 12px 16px;
}

.mode-btn {
  flex: 1;
  padding: 10px 0;
  border-radius: 8px;
  border: 1.5px solid #ddd;
  background: #fff;
  font-size: 14px;
  color: #555;
  cursor: pointer;
}

.mode-btn.active {
  background: #111;
  border-color: #111;
  color: #fff;
}

/* テストカード */
#test-area {
  padding: 16px;
}

.test-question {
  text-align: center;
  font-size: 32px;
  font-weight: 700;
  padding: 32px 16px;
  background: #fff;
  border-radius: 16px;
  margin-bottom: 16px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.08);
}

.test-question .sub {
  font-size: 14px;
  color: #888;
  margin-top: 8px;
}

.choices {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.choice-btn {
  width: 100%;
  padding: 16px;
  border-radius: 12px;
  border: 1.5px solid #e0e0e0;
  background: #fff;
  font-size: 16px;
  text-align: left;
  cursor: pointer;
}

.choice-btn.correct { background: #e6f4ea; border-color: #4caf50; }
.choice-btn.wrong   { background: #fce8e6; border-color: #e53e3e; }

.test-result {
  text-align: center;
  padding: 24px;
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.08);
}

.result-score {
  font-size: 48px;
  font-weight: 700;
  color: #111;
}

.result-label {
  font-size: 15px;
  color: #777;
  margin-bottom: 20px;
}

.btn-primary {
  display: inline-block;
  padding: 12px 32px;
  background: #111;
  color: #fff;
  border: none;
  border-radius: 24px;
  font-size: 16px;
  cursor: pointer;
}

/* 手書きキャンバス */
#canvas-wrap {
  text-align: center;
  margin-top: 12px;
}

#handwrite-canvas {
  border: 2px solid #ddd;
  border-radius: 12px;
  background: #fff;
  touch-action: none;
}

.canvas-btns {
  display: flex;
  gap: 10px;
  justify-content: center;
  margin-top: 12px;
}

.btn-secondary {
  padding: 10px 24px;
  border-radius: 24px;
  border: 1.5px solid #ccc;
  background: #fff;
  font-size: 15px;
  cursor: pointer;
  color: #444;
}

.self-grade {
  display: flex;
  gap: 12px;
  justify-content: center;
  margin-top: 16px;
}

.grade-btn {
  flex: 1;
  max-width: 120px;
  padding: 14px 0;
  border-radius: 12px;
  border: none;
  font-size: 20px;
  cursor: pointer;
}

.grade-btn.ok   { background: #e6f4ea; color: #2e7d32; }
.grade-btn.ng   { background: #fce8e6; color: #c62828; }

/* 履歴 */
.history-card {
  background: #fff;
  border-radius: 12px;
  padding: 14px 16px;
  margin: 8px 16px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.06);
}

.history-date {
  font-size: 12px;
  color: #999;
  margin-bottom: 4px;
}

.history-score {
  font-size: 18px;
  font-weight: 700;
  color: #111;
}

.history-mode {
  font-size: 13px;
  color: #777;
}

.progress-bar-wrap {
  background: #eee;
  border-radius: 4px;
  height: 6px;
  margin-top: 8px;
}

.progress-bar {
  height: 6px;
  border-radius: 4px;
  background: #4caf50;
}

/* スマホ対応 */
@media (max-width: 768px) {
  .card-hangul { font-size: 22px; }
  .test-question { font-size: 28px; }
}
```

- [ ] **Step 2: app.jsの最小構成を作成してタブ切り替えを動かす**

```js
// タブ切り替え
document.querySelectorAll('.nav-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    const tab = btn.dataset.tab;
    document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
    document.querySelectorAll('.tab-content').forEach(s => s.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById('tab-' + tab).classList.add('active');
  });
});
```

ファイルとして保存: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 3: 動作確認**

ブラウザで開き、ボトムナビ4つのタブが切り替わることを確認する

- [ ] **Step 4: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/style.css 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：CSSスタイル・タブナビ実装"
```

---

### Task 3: 単語データ（words.js）

**Files:**
- Create: `03_PRODUCTS/01_韓国語学習/data/words.js`

- [ ] **Step 1: words.jsを作成する（50語）**

```js
const words = [
  // あいさつ
  { id:"w001", hangul:"안녕하세요", reading:"アンニョンハセヨ", japanese:"こんにちは", category:"あいさつ" },
  { id:"w002", hangul:"안녕히 가세요", reading:"アンニョンヒ カセヨ", japanese:"さようなら（去る人に）", category:"あいさつ" },
  { id:"w003", hangul:"안녕히 계세요", reading:"アンニョンヒ ケセヨ", japanese:"さようなら（残る人に）", category:"あいさつ" },
  { id:"w004", hangul:"감사합니다", reading:"カムサハムニダ", japanese:"ありがとうございます", category:"あいさつ" },
  { id:"w005", hangul:"죄송합니다", reading:"チェソンハムニダ", japanese:"申し訳ありません", category:"あいさつ" },
  { id:"w006", hangul:"괜찮아요", reading:"ケンチャナヨ", japanese:"大丈夫です", category:"あいさつ" },
  { id:"w007", hangul:"네", reading:"ネ", japanese:"はい", category:"あいさつ" },
  { id:"w008", hangul:"아니요", reading:"アニヨ", japanese:"いいえ", category:"あいさつ" },
  { id:"w009", hangul:"처음 뵙겠습니다", reading:"チョウム ペプケッスムニダ", japanese:"はじめまして", category:"あいさつ" },
  { id:"w010", hangul:"잘 부탁드립니다", reading:"チャル プタクトゥリムニダ", japanese:"よろしくお願いします", category:"あいさつ" },

  // 数字
  { id:"w011", hangul:"일", reading:"イル", japanese:"1（漢数字）", category:"数字" },
  { id:"w012", hangul:"이", reading:"イ", japanese:"2（漢数字）", category:"数字" },
  { id:"w013", hangul:"삼", reading:"サム", japanese:"3（漢数字）", category:"数字" },
  { id:"w014", hangul:"사", reading:"サ", japanese:"4（漢数字）", category:"数字" },
  { id:"w015", hangul:"오", reading:"オ", japanese:"5（漢数字）", category:"数字" },
  { id:"w016", hangul:"육", reading:"ユク", japanese:"6（漢数字）", category:"数字" },
  { id:"w017", hangul:"칠", reading:"チル", japanese:"7（漢数字）", category:"数字" },
  { id:"w018", hangul:"팔", reading:"パル", japanese:"8（漢数字）", category:"数字" },
  { id:"w019", hangul:"구", reading:"ク", japanese:"9（漢数字）", category:"数字" },
  { id:"w020", hangul:"십", reading:"シプ", japanese:"10（漢数字）", category:"数字" },

  // 日常語
  { id:"w021", hangul:"밥", reading:"パプ", japanese:"ご飯", category:"日常語" },
  { id:"w022", hangul:"물", reading:"ムル", japanese:"水", category:"日常語" },
  { id:"w023", hangul:"커피", reading:"コピ", japanese:"コーヒー", category:"日常語" },
  { id:"w024", hangul:"맥주", reading:"メクチュ", japanese:"ビール", category:"日常語" },
  { id:"w025", hangul:"화장실", reading:"ファジャンシル", japanese:"トイレ", category:"日常語" },
  { id:"w026", hangul:"역", reading:"ヨク", japanese:"駅", category:"日常語" },
  { id:"w027", hangul:"지하철", reading:"チハチョル", japanese:"地下鉄", category:"日常語" },
  { id:"w028", hangul:"버스", reading:"ポス", japanese:"バス", category:"日常語" },
  { id:"w029", hangul:"택시", reading:"テクシ", japanese:"タクシー", category:"日常語" },
  { id:"w030", hangul:"호텔", reading:"ホテル", japanese:"ホテル", category:"日常語" },

  // 動詞
  { id:"w031", hangul:"먹다", reading:"モクタ", japanese:"食べる", category:"動詞" },
  { id:"w032", hangul:"마시다", reading:"マシダ", japanese:"飲む", category:"動詞" },
  { id:"w033", hangul:"가다", reading:"カダ", japanese:"行く", category:"動詞" },
  { id:"w034", hangul:"오다", reading:"オダ", japanese:"来る", category:"動詞" },
  { id:"w035", hangul:"보다", reading:"ポダ", japanese:"見る", category:"動詞" },
  { id:"w036", hangul:"사다", reading:"サダ", japanese:"買う", category:"動詞" },
  { id:"w037", hangul:"말하다", reading:"マラダ", japanese:"話す", category:"動詞" },
  { id:"w038", hangul:"듣다", reading:"トゥッタ", japanese:"聞く", category:"動詞" },
  { id:"w039", hangul:"알다", reading:"アルダ", japanese:"わかる・知る", category:"動詞" },
  { id:"w040", hangul:"좋아하다", reading:"チョアハダ", japanese:"好きだ", category:"動詞" },

  // 形容詞
  { id:"w041", hangul:"맛있다", reading:"マシッタ", japanese:"おいしい", category:"形容詞" },
  { id:"w042", hangul:"맛없다", reading:"マドプタ", japanese:"まずい", category:"形容詞" },
  { id:"w043", hangul:"크다", reading:"クダ", japanese:"大きい", category:"形容詞" },
  { id:"w044", hangul:"작다", reading:"チャクタ", japanese:"小さい", category:"形容詞" },
  { id:"w045", hangul:"비싸다", reading:"ピッサダ", japanese:"高い（値段）", category:"形容詞" },
  { id:"w046", hangul:"싸다", reading:"ッサダ", japanese:"安い", category:"形容詞" },
  { id:"w047", hangul:"덥다", reading:"トプタ", japanese:"暑い", category:"形容詞" },
  { id:"w048", hangul:"춥다", reading:"チュプタ", japanese:"寒い", category:"形容詞" },
  { id:"w049", hangul:"예쁘다", reading:"イェップダ", japanese:"きれいだ・かわいい", category:"形容詞" },
  { id:"w050", hangul:"재미있다", reading:"チェミイッタ", japanese:"おもしろい", category:"形容詞" },
];
```

- [ ] **Step 2: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/data/words.js
git commit -m "韓国語学習：単語データ50語追加"
```

---

### Task 4: フレーズデータ（phrases.js）

**Files:**
- Create: `03_PRODUCTS/01_韓国語学習/data/phrases.js`

- [ ] **Step 1: phrases.jsを作成する（K CHOICE! 30件＋基本20件）**

```js
const phrases = [
  // K CHOICE! フレーズ（ユーザー指定 30件）
  { id:"p001", hangul:"이름이 뭐예요?", reading:"イルミ ムォエヨ？", japanese:"お名前はなんですか？", kchoice:true },
  { id:"p002", hangul:"어디 출신이에요?", reading:"オディ チュルシニエヨ？", japanese:"出身地はどこですか？", kchoice:true },
  { id:"p003", hangul:"어디에 살아요?", reading:"オディエ サラヨ？", japanese:"どこに住んでいますか？", kchoice:true },
  { id:"p004", hangul:"잘 지내요?", reading:"チャル チネヨ？", japanese:"元気ですか？", kchoice:true },
  { id:"p005", hangul:"당신은 어때요?", reading:"タンシヌン オッテヨ？", japanese:"あなたはどうですか？", kchoice:true },
  { id:"p006", hangul:"처음 뵙겠습니다", reading:"チョウム ペプケッスムニダ", japanese:"はじめまして", kchoice:true },
  { id:"p007", hangul:"뭐 해요?", reading:"ムォ ヘヨ？", japanese:"何をしていますか？", kchoice:true },
  { id:"p008", hangul:"○○어를 할 수 있어요?", reading:"○○オルル ハル ス イッソヨ？", japanese:"○○語を話しますか？", kchoice:true },
  { id:"p009", hangul:"왜 ○○어를 공부해요?", reading:"ウェ ○○オルル コンブヘヨ？", japanese:"なぜ○○語を勉強していますか？", kchoice:true },
  { id:"p010", hangul:"왜냐하면 ○○이기 때문이에요", reading:"ウェニャハミョン ○○イギ テムニエヨ", japanese:"なぜなら○○だからです", kchoice:true },
  { id:"p011", hangul:"○○어를 얼마나 배웠어요?", reading:"○○オルル オルマナ ペウォッソヨ？", japanese:"どれくらい○○語を学んでいますか？", kchoice:true },
  { id:"p012", hangul:"취미가 뭐예요?", reading:"チュィミガ ムォエヨ？", japanese:"趣味はなんですか？", kchoice:true },
  { id:"p013", hangul:"○○한 적 있어요?", reading:"○○ハン チョク イッソヨ？", japanese:"○○したことはありますか？", kchoice:true },
  { id:"p014", hangul:"○○해도 돼요?", reading:"○○ヘド トゥェヨ？", japanese:"○○してもいいですか？", kchoice:true },
  { id:"p015", hangul:"언제 ○○했어요?", reading:"オンジェ ○○ヘッソヨ？", japanese:"いつ○○しましたか？", kchoice:true },
  { id:"p016", hangul:"○○을 좋아해요?", reading:"○○ウル チョアヘヨ？", japanese:"○○が好きですか？", kchoice:true },
  { id:"p017", hangul:"○○은 무슨 뜻이에요?", reading:"○○ウン ムスン トゥシエヨ？", japanese:"○○はどういう意味ですか？", kchoice:true },
  { id:"p018", hangul:"○○을 할 수 있어요?", reading:"○○ウル ハル ス イッソヨ？", japanese:"○○ができますか？", kchoice:true },
  { id:"p019", hangul:"○○이 있어요?", reading:"○○イ イッソヨ？", japanese:"○○がありますか？", kchoice:true },
  { id:"p020", hangul:"다시 한번 말해 주세요", reading:"タシ ハンボン マレ ジュセヨ", japanese:"もう一度言ってください", kchoice:true },
  { id:"p021", hangul:"○○은 얼마예요?", reading:"○○ウン オルマエヨ？", japanese:"○○はいくらですか？", kchoice:true },
  { id:"p022", hangul:"○○에 가고 싶어요", reading:"○○エ カゴ シポヨ", japanese:"○○に行きたいです", kchoice:true },
  { id:"p023", hangul:"○○까지 어떻게 가요?", reading:"○○カジ オットケ カヨ？", japanese:"○○まではどうやって行きますか？", kchoice:true },
  { id:"p024", hangul:"어느 ○○이에요?", reading:"オヌ ○○イエヨ？", japanese:"どの○○ですか？", kchoice:true },
  { id:"p025", hangul:"저분은 누구세요?", reading:"チョブヌン ヌグセヨ？", japanese:"あの方はどなたですか？", kchoice:true },
  { id:"p026", hangul:"몇 시예요?", reading:"ミョッ シエヨ？", japanese:"何時ですか？", kchoice:true },
  { id:"p027", hangul:"추천 요리는 뭐예요?", reading:"チュチョン ヨリヌン ムォエヨ？", japanese:"おすすめの料理はなんですか？", kchoice:true },
  { id:"p028", hangul:"이것 주세요", reading:"イゴッ ジュセヨ", japanese:"これをください", kchoice:true },
  { id:"p029", hangul:"계산 부탁드려요", reading:"ケサン プタクトゥリョヨ", japanese:"お会計をお願いします", kchoice:true },
  { id:"p030", hangul:"○○해 주실 수 있어요?", reading:"○○ヘ ジュシル ス イッソヨ？", japanese:"○○していただけますか？", kchoice:true },

  // 基本フレーズ（クロコ追加 20件）
  { id:"p031", hangul:"잠깐만요", reading:"チャムカンマンニョ", japanese:"ちょっと待ってください", kchoice:false },
  { id:"p032", hangul:"이해했어요", reading:"イヘヘッソヨ", japanese:"わかりました", kchoice:false },
  { id:"p033", hangul:"모르겠어요", reading:"モルゲッソヨ", japanese:"わかりません", kchoice:false },
  { id:"p034", hangul:"천천히 말해 주세요", reading:"チョンチョニ マレ ジュセヨ", japanese:"ゆっくり話してください", kchoice:false },
  { id:"p035", hangul:"한국어를 조금 해요", reading:"ハングゴルル チョグム ヘヨ", japanese:"韓国語が少しできます", kchoice:false },
  { id:"p036", hangul:"일본어를 할 수 있어요?", reading:"イルボノルル ハル ス イッソヨ？", japanese:"日本語を話せますか？", kchoice:false },
  { id:"p037", hangul:"도와주세요", reading:"トワジュセヨ", japanese:"助けてください", kchoice:false },
  { id:"p038", hangul:"맛있어요!", reading:"マシッソヨ！", japanese:"おいしいです！", kchoice:false },
  { id:"p039", hangul:"어디에 있어요?", reading:"オディエ イッソヨ？", japanese:"どこにありますか？", kchoice:false },
  { id:"p040", hangul:"얼마나 걸려요?", reading:"オルマナ コルリョヨ？", japanese:"どのくらいかかりますか？", kchoice:false },
  { id:"p041", hangul:"사진 찍어도 돼요?", reading:"サジン チゴド トゥェヨ？", japanese:"写真を撮ってもいいですか？", kchoice:false },
  { id:"p042", hangul:"화장실이 어디예요?", reading:"ファジャンシリ オディエヨ？", japanese:"トイレはどこですか？", kchoice:false },
  { id:"p043", hangul:"영수증 주세요", reading:"ヨンスジュン ジュセヨ", japanese:"領収書をください", kchoice:false },
  { id:"p044", hangul:"카드로 해도 돼요?", reading:"カドゥロ ヘド トゥェヨ？", japanese:"カードで払えますか？", kchoice:false },
  { id:"p045", hangul:"한 개 더 주세요", reading:"ハン ゲ ト ジュセヨ", japanese:"もう一つください", kchoice:false },
  { id:"p046", hangul:"괜찮아요?", reading:"ケンチャナヨ？", japanese:"大丈夫ですか？", kchoice:false },
  { id:"p047", hangul:"좋아요!", reading:"チョアヨ！", japanese:"いいですね！", kchoice:false },
  { id:"p048", hangul:"축하해요", reading:"チュカヘヨ", japanese:"おめでとうございます", kchoice:false },
  { id:"p049", hangul:"맛집 추천해 주세요", reading:"マッチプ チュチョンヘ ジュセヨ", japanese:"おすすめのお店を教えてください", kchoice:false },
  { id:"p050", hangul:"또 만나요", reading:"ト マンナヨ", japanese:"またお会いしましょう", kchoice:false },
];
```

- [ ] **Step 2: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/data/phrases.js
git commit -m "韓国語学習：フレーズデータ50件追加（K CHOICE! 30件）"
```

---

### Task 5: 単語タブ（フラッシュカード・フィルター・覚えたボタン）

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 1: app.jsにlocalStorage管理とwords描画を追記する**

app.jsを以下の内容で置き換える（タブ切り替えコードも含む）：

```js
// ---- localStorage管理 ----
function getProgress() {
  return JSON.parse(localStorage.getItem('kr-progress') || '{}');
}
function saveProgress(data) {
  localStorage.setItem('kr-progress', JSON.stringify(data));
}
function getHistory() {
  return JSON.parse(localStorage.getItem('kr-history') || '[]');
}
function saveHistory(data) {
  localStorage.setItem('kr-history', JSON.stringify(data));
}

// ---- タブ切り替え ----
document.querySelectorAll('.nav-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    const tab = btn.dataset.tab;
    document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
    document.querySelectorAll('.tab-content').forEach(s => s.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById('tab-' + tab).classList.add('active');
    if (tab === 'words') renderWords(currentCategory);
    if (tab === 'phrases') renderPhrases();
    if (tab === 'history') renderHistory();
  });
});

// ---- 単語タブ ----
let currentCategory = 'すべて';

function renderWords(category) {
  const progress = getProgress();
  const container = document.getElementById('words-list');

  // カテゴリ一覧
  const categories = ['すべて', ...new Set(words.map(w => w.category))];
  const filterBar = `<div class="filter-bar">${
    categories.map(c => `<button class="filter-btn${c === category ? ' active' : ''}" data-cat="${c}">${c}</button>`).join('')
  }</div>`;

  // フィルター済み単語
  const filtered = category === 'すべて' ? words : words.filter(w => w.category === category);

  const cards = filtered.map(w => {
    const p = progress[w.id] || {};
    const learned = p.learned ? 'learned' : '';
    const learnedLabel = p.learned ? '✓ 覚えた' : '覚えた';
    return `
      <div class="card">
        <div class="card-hangul">${w.hangul}</div>
        <div class="card-reading">${w.reading}</div>
        <div class="card-japanese">${w.japanese}</div>
        <button class="btn-learned ${learned}" data-id="${w.id}">${learnedLabel}</button>
      </div>`;
  }).join('');

  container.innerHTML = filterBar + cards;

  // フィルターボタン
  container.querySelectorAll('.filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      currentCategory = btn.dataset.cat;
      renderWords(currentCategory);
    });
  });

  // 覚えたボタン
  container.querySelectorAll('.btn-learned').forEach(btn => {
    btn.addEventListener('click', () => {
      const id = btn.dataset.id;
      const progress = getProgress();
      if (!progress[id]) progress[id] = { correct:0, wrong:0, lastStudied:'' };
      progress[id].learned = !progress[id].learned;
      saveProgress(progress);
      renderWords(currentCategory);
    });
  });
}

// 初期表示
renderWords('すべて');
```

- [ ] **Step 2: 動作確認**

ブラウザで単語タブを開き、カード一覧・フィルター切り替え・「覚えた」ボタンが機能することを確認する

- [ ] **Step 3: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：単語タブ実装（フラッシュカード・フィルター・覚えた）"
```

---

### Task 6: フレーズタブ（K CHOICE!バッジ）

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 1: renderPhrasesをapp.jsに追記する**

```js
// ---- フレーズタブ ----
function renderPhrases() {
  const progress = getProgress();
  const container = document.getElementById('phrases-list');

  const cards = phrases.map(p => {
    const prog = progress[p.id] || {};
    const learned = prog.learned ? 'learned' : '';
    const learnedLabel = prog.learned ? '✓ 覚えた' : '覚えた';
    const badge = p.kchoice ? '<span class="badge-kchoice">K CHOICE!</span>' : '';
    return `
      <div class="card">
        <div class="card-hangul">${p.hangul}${badge}</div>
        <div class="card-reading">${p.reading}</div>
        <div class="card-japanese">${p.japanese}</div>
        <button class="btn-learned ${learned}" data-id="${p.id}">${learnedLabel}</button>
      </div>`;
  }).join('');

  container.innerHTML = cards;

  container.querySelectorAll('.btn-learned').forEach(btn => {
    btn.addEventListener('click', () => {
      const id = btn.dataset.id;
      const progress = getProgress();
      if (!progress[id]) progress[id] = { correct:0, wrong:0, lastStudied:'' };
      progress[id].learned = !progress[id].learned;
      saveProgress(progress);
      renderPhrases();
    });
  });
}
```

- [ ] **Step 2: 動作確認**

フレーズタブを開き、K CHOICE!バッジが赤く表示されていること・覚えたボタンが動くことを確認する

- [ ] **Step 3: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：フレーズタブ実装（K CHOICE!バッジ）"
```

---

### Task 7: テスト「日→韓」「韓→日」モード

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 1: テストロジックをapp.jsに追記する**

```js
// ---- テストタブ ----
let currentTestMode = 'ja-to-ko';
let testItems = [];
let testIndex = 0;
let testCorrect = 0;

document.querySelectorAll('.mode-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    document.querySelectorAll('.mode-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    currentTestMode = btn.dataset.mode;
    startTest();
  });
});

function startTest() {
  const all = [...words, ...phrases];
  testItems = shuffle(all).slice(0, 10);
  testIndex = 0;
  testCorrect = 0;
  renderTestQuestion();
}

function shuffle(arr) {
  return [...arr].sort(() => Math.random() - 0.5);
}

function renderTestQuestion() {
  const area = document.getElementById('test-area');
  if (testIndex >= testItems.length) {
    showTestResult();
    return;
  }
  if (currentTestMode === 'handwrite') {
    renderHandwrite();
    return;
  }

  const item = testItems[testIndex];
  const isJaToKo = currentTestMode === 'ja-to-ko';
  const question = isJaToKo ? item.japanese : item.hangul;
  const answerKey = isJaToKo ? 'hangul' : 'japanese';

  // 選択肢（正解1＋ランダム3）
  const all = [...words, ...phrases];
  const wrongs = shuffle(all.filter(i => i.id !== item.id)).slice(0, 3);
  const choices = shuffle([item, ...wrongs]);

  const badge = item.kchoice ? '<span class="badge-kchoice">K CHOICE!</span>' : '';

  area.innerHTML = `
    <div class="test-question">
      ${question}${badge}
      <div class="sub">${testIndex + 1} / ${testItems.length}</div>
    </div>
    <div class="choices">
      ${choices.map(c => `<button class="choice-btn" data-id="${c.id}" data-correct="${c.id === item.id}">${c[answerKey]}</button>`).join('')}
    </div>`;

  area.querySelectorAll('.choice-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const isCorrect = btn.dataset.correct === 'true';
      area.querySelectorAll('.choice-btn').forEach(b => {
        b.disabled = true;
        if (b.dataset.correct === 'true') b.classList.add('correct');
        else if (b === btn && !isCorrect) b.classList.add('wrong');
      });
      recordResult(item.id, isCorrect);
      if (isCorrect) testCorrect++;
      setTimeout(() => {
        testIndex++;
        renderTestQuestion();
      }, 1000);
    });
  });
}

function recordResult(id, isCorrect) {
  const progress = getProgress();
  if (!progress[id]) progress[id] = { correct:0, wrong:0, lastStudied:'' };
  if (isCorrect) progress[id].correct++;
  else progress[id].wrong++;
  progress[id].lastStudied = new Date().toLocaleDateString('ja-JP');
  saveProgress(progress);
}

function showTestResult() {
  const area = document.getElementById('test-area');
  const modeLabel = { 'ja-to-ko':'日→韓', 'ko-to-ja':'韓→日', 'handwrite':'手書き' }[currentTestMode];
  area.innerHTML = `
    <div class="test-result">
      <div class="result-score">${testCorrect} / ${testItems.length}</div>
      <div class="result-label">${modeLabel} テスト結果</div>
      <button class="btn-primary" id="retry-btn">もう一度</button>
    </div>`;

  // 履歴に保存
  const history = getHistory();
  history.unshift({
    date: new Date().toLocaleDateString('ja-JP'),
    mode: modeLabel,
    score: testCorrect,
    total: testItems.length
  });
  saveHistory(history.slice(0, 50));

  document.getElementById('retry-btn').addEventListener('click', startTest);
}
```

- [ ] **Step 2: 動作確認**

テストタブを開き、「日→韓」「韓→日」で10問出題→正解・不正解の色変化→結果表示の流れを確認する

- [ ] **Step 3: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：テスト日→韓・韓→日モード実装"
```

---

### Task 8: テスト「手書き」モード（Canvasキャンバス）

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 1: renderHandwriteをapp.jsに追記する**

```js
// ---- 手書きモード ----
function renderHandwrite() {
  const area = document.getElementById('test-area');
  const item = testItems[testIndex];
  const badge = item.kchoice ? '<span class="badge-kchoice">K CHOICE!</span>' : '';

  area.innerHTML = `
    <div class="test-question">
      ${item.japanese}${badge}
      <div class="sub">${testIndex + 1} / ${testItems.length} — ハングルで書いてみよう</div>
    </div>
    <div id="canvas-wrap">
      <canvas id="handwrite-canvas" width="300" height="200"></canvas>
      <div class="canvas-btns">
        <button class="btn-secondary" id="clear-canvas">消す</button>
        <button class="btn-primary" id="show-answer">答えを見る</button>
      </div>
    </div>
    <div id="answer-area"></div>`;

  // キャンバス描画
  const canvas = document.getElementById('handwrite-canvas');
  const ctx = canvas.getContext('2d');
  ctx.strokeStyle = '#111';
  ctx.lineWidth = 3;
  ctx.lineCap = 'round';
  let drawing = false;

  function getPos(e) {
    const rect = canvas.getBoundingClientRect();
    const touch = e.touches ? e.touches[0] : e;
    return {
      x: (touch.clientX - rect.left) * (canvas.width / rect.width),
      y: (touch.clientY - rect.top) * (canvas.height / rect.height)
    };
  }

  canvas.addEventListener('mousedown',  e => { drawing = true; ctx.beginPath(); const p = getPos(e); ctx.moveTo(p.x, p.y); });
  canvas.addEventListener('mousemove',  e => { if (!drawing) return; const p = getPos(e); ctx.lineTo(p.x, p.y); ctx.stroke(); });
  canvas.addEventListener('mouseup',    () => drawing = false);
  canvas.addEventListener('touchstart', e => { e.preventDefault(); drawing = true; ctx.beginPath(); const p = getPos(e); ctx.moveTo(p.x, p.y); }, { passive:false });
  canvas.addEventListener('touchmove',  e => { e.preventDefault(); if (!drawing) return; const p = getPos(e); ctx.lineTo(p.x, p.y); ctx.stroke(); }, { passive:false });
  canvas.addEventListener('touchend',   () => drawing = false);

  document.getElementById('clear-canvas').addEventListener('click', () => {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
  });

  document.getElementById('show-answer').addEventListener('click', () => {
    const answerArea = document.getElementById('answer-area');
    answerArea.innerHTML = `
      <div style="text-align:center; padding:16px; background:#fff; border-radius:12px; margin-top:12px;">
        <div style="font-size:36px; font-weight:700;">${item.hangul}</div>
        <div style="font-size:14px; color:#888; margin-top:4px;">${item.reading}</div>
        <div class="self-grade">
          <button class="grade-btn ok" data-correct="true">○</button>
          <button class="grade-btn ng" data-correct="false">✕</button>
        </div>
      </div>`;

    answerArea.querySelectorAll('.grade-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const isCorrect = btn.dataset.correct === 'true';
        recordResult(item.id, isCorrect);
        if (isCorrect) testCorrect++;
        testIndex++;
        renderTestQuestion();
      });
    });
  });
}
```

- [ ] **Step 2: 動作確認**

手書きモードでキャンバスに描けること・消去ボタン・答えを見る→○×自己採点の流れを確認する

- [ ] **Step 3: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：手書きテストモード実装（Canvas・自己採点）"
```

---

### Task 9: 履歴タブ

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`

- [ ] **Step 1: renderHistoryをapp.jsに追記する**

```js
// ---- 履歴タブ ----
function renderHistory() {
  const history = getHistory();
  const progress = getProgress();
  const area = document.getElementById('history-area');

  // 全体統計
  const allIds = [...words, ...phrases].map(i => i.id);
  let totalCorrect = 0, totalWrong = 0;
  allIds.forEach(id => {
    const p = progress[id] || {};
    totalCorrect += p.correct || 0;
    totalWrong += p.wrong || 0;
  });
  const total = totalCorrect + totalWrong;
  const rate = total > 0 ? Math.round(totalCorrect / total * 100) : 0;
  const learnedCount = allIds.filter(id => (progress[id] || {}).learned).length;

  const summary = `
    <div class="history-card">
      <div style="font-size:13px; color:#999; margin-bottom:8px;">累計成績</div>
      <div style="font-size:28px; font-weight:700; color:#111;">${rate}%</div>
      <div style="font-size:13px; color:#777; margin-bottom:8px;">${totalCorrect}問正解 / ${total}問</div>
      <div class="progress-bar-wrap"><div class="progress-bar" style="width:${rate}%"></div></div>
      <div style="font-size:13px; color:#777; margin-top:10px;">覚えた単語・フレーズ：${learnedCount}件</div>
    </div>`;

  const logs = history.length === 0
    ? '<p style="padding:16px; color:#999;">まだテスト履歴がありません</p>'
    : history.map(h => `
      <div class="history-card">
        <div class="history-date">${h.date}</div>
        <div class="history-score">${h.score} / ${h.total}</div>
        <div class="history-mode">${h.mode}</div>
        <div class="progress-bar-wrap" style="margin-top:8px;">
          <div class="progress-bar" style="width:${Math.round(h.score/h.total*100)}%"></div>
        </div>
      </div>`).join('');

  area.innerHTML = summary + '<div style="height:8px;"></div>' + logs;
}
```

- [ ] **Step 2: 動作確認**

テストを1回実施後に履歴タブを開き、累計成績・テスト履歴が表示されることを確認する

- [ ] **Step 3: コミット**

```bash
git add 03_PRODUCTS/01_韓国語学習/app.js
git commit -m "韓国語学習：履歴タブ実装（累計成績・テスト履歴）"
```

---

### Task 10: テスト初期起動・iPhone Safari最終調整

**Files:**
- Modify: `03_PRODUCTS/01_韓国語学習/app.js`
- Modify: `03_PRODUCTS/01_韓国語学習/index.html`
- Modify: `03_PRODUCTS/01_韓国語学習/style.css`

- [ ] **Step 1: app.jsの末尾にテスト初期起動を追記する**

```js
// テストタブ初期起動
startTest();
```

- [ ] **Step 2: index.htmlのheadにPWA用metaとiPhone対応を追記する**

headタグ内に追加：
```html
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="theme-color" content="#ffffff">
```

- [ ] **Step 3: style.cssにiPhone SafeAreaとfont-size確認を追記する**

```css
/* iPhone SafeArea対応 */
#bottom-nav {
  padding-bottom: env(safe-area-inset-bottom);
  height: calc(72px + env(safe-area-inset-bottom));
}

body {
  padding-bottom: calc(80px + env(safe-area-inset-bottom));
}

/* input がある場合は必ず16px以上 */
input, textarea {
  font-size: 16px;
}
```

- [ ] **Step 4: iPhoneのSafariで動作確認**

1. ローカルサーバーまたはGitHub Pages URLをiPhoneのSafariで開く
2. 全タブが正常に切り替わること
3. テスト・手書きキャンバスが指で操作できること
4. ボトムナビがホームインジケーターと重ならないこと

- [ ] **Step 5: 最終コミット・push**

```bash
git add 03_PRODUCTS/01_韓国語学習/
git commit -m "韓国語学習：iPhone Safari対応・最終調整"
git push
```

---

## 完成チェックリスト

- [ ] 単語50語が一覧表示・フィルタリングできる
- [ ] フレーズ50件が表示される（K CHOICE! 30件にバッジあり）
- [ ] 覚えたボタンが単語・フレーズ両方で動く
- [ ] テスト「日→韓」10問で正解/不正解の色変化・結果表示
- [ ] テスト「韓→日」同上
- [ ] テスト「手書き」でキャンバスに書ける・答えを見る・自己採点できる
- [ ] 履歴タブに累計成績・テスト履歴が表示される
- [ ] iPhone Safariでボトムナビが操作しやすい
- [ ] K CHOICE!バッジが単語・フレーズ・テスト全画面で表示される
